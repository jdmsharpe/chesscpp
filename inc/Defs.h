#ifndef DEFS_H
#define DEFS_H

#include <array>
#include <iostream>
#include <memory>
#include <string>
#include <utility>

constexpr int k_totalPieces = 32;

enum class Color { black, white };

typedef std::pair<int, int> Position;

#endif // DEFS_H
